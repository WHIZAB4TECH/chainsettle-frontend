c # Task: Display role badge for current user on shipment detail page

## Steps

- [x] 1. Analyze codebase and create plan
- [x] 2. Add `deriveUserRole()` and `roleBadge()` to `src/lib/utils/index.ts`
- [x] 3. Update `src/app/dashboard/shipments/[id]/page.tsx` to use shared util and render badge
- [x] 4. Implemented — build verification requires `npm install` + `npm run build`
## Steps

- [x] 1. Analyze codebase and create plan
- [ ] 2. Add `deriveUserRole()` and `roleBadge()` to `src/lib/utils/index.ts`
- [ ] 3. Update `src/app/dashboard/shipments/[id]/page.tsx` to use shared util and render badge
- [ ] 4. Verify build succeeds

